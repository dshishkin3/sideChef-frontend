import React, { FC } from "react";
import Cuisines from "../components/cuisines/Cuisines";

const Cuisine: FC = () => {
  return (
    <div>
      <Cuisines />
    </div>
  );
};

export default Cuisine;
